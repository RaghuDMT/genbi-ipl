# =============================================================================
# genbi-ipl — Intelligence Service Dependencies
# =============================================================================
# CPU-only PyTorch is forced via the extra index URL to avoid the 2GB CUDA
# build. sentence-transformers only needs CPU for localhost embedding generation.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core service ──────────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
python-dotenv>=1.0,<2.0
structlog>=24.4,<26.0

# ── ETL ───────────────────────────────────────────────────────────────────────
duckdb>=1.1,<2.0
pandas>=2.0,<4.0

# ── Phase 2: Text-to-SQL pipeline ─────────────────────────────────────────────
langchain>=0.3,<0.4
langchain-groq>=0.2,<0.3
groq>=0.11,<1.0
sqlglot>=25.0,<27.0
PyYAML>=6.0,<7.0
Jinja2>=3.1,<4.0

# ── Phase 3: RAG / vector retrieval ───────────────────────────────────────────
chromadb>=0.5,<1.0
sentence-transformers>=3.0,<4.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.3,<9.0